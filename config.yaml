# Non-sensitive configuration defaults.
# Secrets go in .env (never committed to git).

server:
  host: "0.0.0.0"
  port: 8665
  reload: true

cors:
  origins:
    - "http://localhost:5173"
    - "http://localhost:8006"

frontend:
  host: "0.0.0.0"
  dev_port: 8006

logging:
  log_dir: "logs"
